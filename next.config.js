/** @type {import('next').NextConfig} */
const nextConfig = {
  // Specify your source directory
  srcDir: 'src',
 // Configure images source
 images: {
  domains: ['images.pexels.com'], // Add the Pexels domain here
},
}
module.exports = nextConfig
